{
  "name": "smartiz",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "preview": "turbo preview",
    "db:generate": "pnpm --filter @smartiz/db db:generate",
    "db:migrate": "pnpm --filter @smartiz/db db:migrate",
    "db:studio": "pnpm --filter @smartiz/db db:studio",
    "db:seed-admin": "pnpm --filter @smartiz/db db:seed-admin",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d",
    "deploy:api": "pnpm --filter @smartiz/api deploy",
    "deploy:web": "pnpm --filter @smartiz/web build && wrangler pages deploy apps/web/dist --project-name=smartizapp-web",
    "deploy:admin": "pnpm --filter @smartiz/admin build && wrangler pages deploy apps/admin/dist --project-name=smartizapp-admin",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "turbo": "^2",
    "typescript": "^5"
  },
  "packageManager": "pnpm@11.1.2"
}
